import React from "react";
import { SectionArticle, SectionTitle } from "../components";

const Blog = () => {
    return (
        <>
            <SectionTitle />
            <SectionArticle />
        </>
    );
};

export default Blog;
